# vLLM Studio / OmniStudio 功能需求文档（OMLX 对照版）

> 版本：v2.0　更新：2026-09-08　范围：本地大模型服务管理一体化
> 参考样本：OMLX 0.6.3（macOS / Apple Silicon / MLX 后端）界面截图 8 张
> 阅读方式：本文档是"目标需求 + 落地状态"双轨；每个模块同时给出需求描述、当前落地情况与代码位置、遗留差距。

---

## 0. 状态图例与更新记录

| 图例 | 含义 |
|---|---|
| ✅ | 已实现并通过代码核对 |
| 🟡 | 部分实现（标注缺什么） |
| ❌ | 未实现（规划中） |

- **v2.0（本次）**：与当前代码对齐。新增三引擎抽象、多 App 结构、聊天系统、下载管理器、基准测试、集成 Launcher、更新通道、i18n 等已落地模块；更新差距分析与路线图。
- v1.0：初始整理（Phase 1 交付）。

---

## 1. 背景与目标

用户提供了 8 张界面截图，内容为 **OMLX**（一款 macOS 上以 MLX 为后端的本地大模型服务管理应用）的完整设置与运维界面。目标：

1. 识别截图对应的功能（见 §2）；
2. 整理为需求文档（本文档）；
3. 将相关功能整合进当前项目 vLLM Studio（Electrobun + Bun 桌面应用），把产品形态升级为"本地大模型一体化管理平台"（OmniStudio 方向），对齐 OMLX 的运维能力。

**核心差异化（已演进）**：OMLX 使用 MLX 后端；本应用使用统一的**运行时抽象**（`apps/studio/src/bun/runtimes/`）支持三种本地推理引擎 + 远程 OpenAI 兼容 API：

| 引擎 | 落地 | 说明 |
|---|---|---|
| llama.cpp（llama-server） | ✅ | 默认引擎，GGUF 模型（本地文件或 `-hf` 拉取），含显存 offload、KV 缓存量化、多模态（mmproj） |
| vLLM | 🟡 | `runtimes/vllm.ts` 已实现参数组装，需环境有 vllm 可执行文件 |
| SGLang | 🟡 | `runtimes/sglang.ts` 已实现参数组装，同样依赖外部环境 |
| 远程（OpenAI 兼容） | ✅ | `SERVER_MODE=remote`，Base URL / API Key / 模型名直连 |

---

## 2. 截图识别结果

8 张截图均为 OMLX 0.6.3 界面（1920×1292，macOS），左侧为分类导航，右侧为对应配置/统计面板。

| # | 截图（左侧导航选中项） | 识别出的功能内容 |
|---|---|---|
| 1 | **服务统计（仪表盘）** | 服务状态、监听地址与端口、重启/停止；预填充/生成 Token、缓存效率；平均处理/生成速度 tok/s；当前活跃（已加载模型列表，含显存占用）；系统信息（GPU 锁定内存、系统 RAM、GPU 使用率、热量、服务器运行时长、版本）；运行时缓存（块大小、文件数、总大小、位置、清除 SSD 缓存）；更新（通道/自动检查/通知） |
| 2 | **网络（服务）** | 监听地址、端口；OpenAI 兼容 `/v1`、Anthropic/Claude Code 端点、`/health`、Prometheus `/metrics`；生成参数（上下文/温度/TopP/TopK/重复惩罚）；启动自动启动；日志级别；存储（基础路径、模型目录多路径，第一个为下载目标） |
| 3 | **性能** | 调度器（最大并发、嵌入批处理、分块预填充、预填充优先级）；内存与生命周期（预填充内存防护、防护层级、Metal 上限、空闲超时自动卸载、模型回退路由）；缓存（KV 缓存溢出：启用/仅热缓存/热缓存大小/SSD 目录与大小/初始缓存块） |
| 4 | **外观** | 菜单栏刷新间隔、是否显示 Dock 图标；菜单栏模型库可见性（仅收藏/全部）；菜单栏项目（LIV/AVG/ALL + CPU/GPU/内存条形图） |
| 5 | **吞吐量（基准测试）** | 硬件信息；可用模型数；模型选择（可未加载、按需加载）；上下文长度扫描（1K–200K）、生成长度、批次大小、Warm-up、ANE 对齐提示 |
| 6 | **模型** | 模型库管理页（列表/详情） |
| 7 | **集成** | Claude Code：模式（云端/本地）、Opus/Sonnet/Haiku 模型映射、上下文缩放、设置命令、直接运行；Codex、桌面应用、OpenCode、OpenClaw、Hermes Agent、Pi、Copilot CLI，每一类可配置默认模型与启动器命令 |
| 8 | **日志** | 服务器日志查看器：行数、复制、日志文件切换、最近 N 条、实时输出 |
| 9 | **（产品自身结构参照）** | OmniStudio 按能力分 **App**：对话 / 语音 / 图片 / OCR；模型按能力分类并绑定到对应 App |

左侧导航全量模块清单：**服务统计、外观、网络、性能、日志、模型、下载器、集成、量化、基准测试（吞吐量/准确度）、上下文、通用、安全、关于**。

---

## 3. 需求优先级总览（含落地状态）

| 优先级 | 模块 | 状态 | 说明 |
|---|---|---|---|
| P0 | 服务统计（仪表盘） | ✅ | 统计页 + 2s 轮询，后端 `bun/stats.ts` |
| P0 | 模型分类 | ✅ | 四分类 + 持久化 + 已安装徽章/筛选 + App 关联 |
| P0 | 网络/服务配置 | ✅ | 三引擎选择、本地/远程、端点展示、自动启动、测试连接 |
| P0 | 模型库 + 下载器 | ✅ | ModelScope 搜索/详情/下载、队列管理、多目录、收藏、续传 |
| P0 | 多 App 结构 + 聊天 | ✅ | 对话/语音/图片/OCR 四 App 切换，会话按 App 隔离 |
| P1 | 集成（Claude Code / Codex 等） | ✅ | 启动命令生成 + 默认模型映射 |
| P1 | 基准测试（吞吐量） | ✅ | 上下文扫描 TTFT/TPOT/TPS，结果表格 + 图表 |
| P1 | 性能（调度/内存/缓存） | 🟡 | 调度器 + KV 缓存量化已归组；**内存与生命周期、空闲卸载、模型回退、缓存分层未落地** |
| P1 | 日志查看器 | 🟡 | 行数/复制/清空/实时已补；**日志文件切换、最近 N 条筛选未做** |
| P2 | 更新通道 / 关于 | ✅ | 稳定/测试通道、关于信息块 |
| P2 | 界面多语言 | ✅ | 中/英切换（i18n） |
| P2 | 外观（菜单栏/Dock） | ❌ | 依赖 Electrobun 托盘能力评估 |
| P2 | 安全（API Key 加密 / 日志脱敏） | ❌ | 规划中 |

---

## 4. 详细需求

### 4.1 P0 服务统计（仪表盘）✅

**需求**：服务状态、监听地址与端口、重启/停止；预填充/生成 Token 与速度；活跃/已加载模型；系统内存与 CPU 负载；服务器运行时长；模型磁盘占用；实时刷新。

**落地**（Phase 1）：
- 后端 `bun/stats.ts`：会话级统计（预填充/生成 token、请求次数、两者平均 tok/s、活跃模型、loadavg/内存、全部已装 GGUF 磁盘占用合计）；`recordUsage()` 由 `chat.ts` 与 OCR vLLM 管线埋点上报；`markServerStarted()` 由运行时在健康检查通过时调用；真实已加载模型通过 `GET /slots` 读取。
- 前端 `mainview/app/server-stats.tsx`：状态徽章、服务器运行时长、6 张统计卡、内存进度条、CPU 负载、活跃模型列表（已加载/空闲徽章），`refetchInterval: 2000`。
- RPC：`getServerStats`；路由 `stats`；i18n 中文/英文完整翻译。

**遗留差距**：
- 无按模型的显存/VRAM 占用（OMLX 有）；`/slots` 不可用时回退为"最近使用即视为已加载"。
- 无 GPU 温度/显存锁定量。

### 4.2 P0 网络/服务配置 ✅

**需求**：监听地址、端口、运行模式、端点展示、自动启动、生成参数、测试连接、保存后重启。

**落地**：
- 设置页 `settings.tsx` 按标签重组（网络/模型/模型库/性能/集成/基准/日志/统计/通用/界面）。
- **推理引擎**（`INFERENCE_ENGINE`）：llama.cpp / vLLM / SGLang，由 `bun/runtimes/` 抽象统一管理（`Runtime` 接口：start/stop/restart/forceKill/status/logs/事件订阅；`server-manager.ts` 作为门面，引擎切换时自动重建并重挂监听）。
- 运行模式：本地（内置引擎）/ 远程（OpenAI 兼容，Base URL / API Key / 模型名 + 测试连接）。
- 端点展示：Chat Completions、`/health`、`/metrics`（未启用置灰），带一键复制。
- 启动时自动启动（`AUTO_START_SERVER`）、保存提示重启（`settings.restartHint`）。
- 高级参数按引擎分组：llama.cpp（image max tokens/temp/topP）、vLLM（model len/tensor parallel/GPU util/dtype/eager）、SGLang（context/TP/mem fraction/chunked prefill）。

**遗留差距**：Anthropic/Claude Code 端点展示未单独列出（并入集成页）；无"默认配置文件回退"概念；TopK、repeat penalty 未暴露为 UI 设置（repeat penalty 目前取自模型 profile 的 `serverArgs`）。

### 4.3 P0 模型库 + 下载器 ✅

**需求**：搜索/预览/下载 ModelScope 模型、下载任务管理（并发/暂停/继续/取消/进度）、中断恢复、多目录、收藏。

**落地**：
- 搜索 `bun/modelscope.ts` `searchModels`（openapi v1，分页）、仓库文件列表 `listRepoFiles`（仅 GGUF）、下载 `downloadFile`。
- **断点续传已修复**：仅当服务器回 `206` 且本地有部分文件时才以 append 续传；回 `200` 则整体覆写（修复了旧版 append 导致文件损坏的 200 分支 bug）；`416` 视为已下载完成。暂停/中断时 flush 部分字节到磁盘，可从精确文件大小续传。
- **下载管理器** `bun/download-manager.ts`：内存态任务队列（并发上限 2），任务会脱离 RPC 请求运行以支撑大文件；状态机 queued/downloading/paused/completed/failed/canceled；速度/百分比节流上报；前端 `download-panel.tsx` 顶栏按钮 + 任务列表（暂停/继续/取消/删除/清理已完成）。
- **模型详情页** `model-detail.tsx`：模型信息（参数/大小/下载量/点赞/许可/tags）、推荐权重文件一键下载（preset 有 defaultQuant 时按量化匹配，否则取最大权重文件）、文件列表逐文件下载/暂停/继续/重试。
- **全格式下载（对齐多引擎）**：文件列表不再只放行 GGUF——vLLM / SGLang 用的 **safetensors**、TTS/ASR 的 bin/pt/onnx 等全部可下载；列表按"权重优先 + 大小降序"排序并打格式徽章（GGUF → llama.cpp，safetensors → vLLM / SGLang），另有格式与引擎对照提示；提供**"下载全部"** 一键拉取整个仓库（vLLM 需要 safetensors+config+tokenizer 全套）。已安装扫描同样识别所有权重格式（safetensors/bin/pt/ckpt/onnx 等），元数据清理逻辑同步放宽。
- **目录多路径**：`model-store.ts` `getModelsDirs()` = 主目录（下载目标，dev 为 `vllm-studio-models`，生产为 userData/models）+ `MODEL_DIRS` 逗号分隔扩展目录（只读发现）。
- **收藏**：`FAVORITE_MODELS` 设置持久化，`toggleFavorite`/`isFavorite`，"仅收藏"筛选。
- **已安装管理**：列表（分类徽章/使用中/收藏/激活/删除），激活后写入 `LOCAL_MODEL_PATH/LOCAL_MODEL_NAME/CHAT_MODEL`，删除时同步清理元数据与设置。

**遗留差距**：任务队列为内存态（重启丢失活跃/排队任务，已下载完成文件不受影响）；扩展目录为文本输入而非目录选择器；无"全部文件/目录（含非 GGUF 如 tokenizer）"下载。

### 4.4 P0 模型分类 ✅

**需求**：模型按能力分类（对话/VLM、TTS、ASR、图像、其他）；下载时持久化；已安装列表徽章与筛选；与 App 绑定。

**落地**：
- 分类体系与共享常量 `shared/modelscope.ts`：`MODEL_CATEGORIES`（chat/tts/asr/image/other）、`classifyModel`（按 ModelScope tags/tasks/模型 ID 关键词自动判定）、`matchCategory`。
- **下载时持久化**：`startModelDownload` 携带 category，完成后写入模型目录下 `.vllm-meta.json`（`setModelCategory`）；已安装读取优先元数据，无元数据时按文件名启发式兜底（`classifyInstalledFilename`）。
- 已安装列表分类徽章 + 分类 chips 筛选 + 仅收藏；搜索/推荐结果同样按分类筛选。
- 内置推荐（`MODEL_PRESETS`）：对话 Qwen2.5/2.5-3B/Qwen3-8B/Qwen3-4B/CogVLM2-19B、TTS CosyVoice2、ASR SenseVoiceSmall/Whisper-large-v3、生图 SDXL/Kolors。

### 4.5 P0 多 App 结构 + 聊天 ✅

**需求（产品结构）**：OmniStudio 按能力分 App——对话 / 语音 / 图片 / OCR；模型分类对应到 App；各 App 会话独立。

**落地**：
- 前端 `stores/app.ts`：`AppId = chat | voice | image | ocr`；侧边栏 4 宫格切换（`app-sidebar.tsx` AppSwitcher），切换即重置会话上下文；语音/图片/OCR App 名称与图标齐全。
- **会话隔离**：`conversations.app` 字段（迁移 0002）；`chat.ts` 的 `listConversations(app)` / `createConversation(app)` 按 App 过滤；侧边栏每 App 独立会话列表；OCR App 显示文档记录列表（无限滚动 + 搜索 + 处理进度环）。
- **聊天** `chat.ts` + `chat-screen.tsx`：流式对话（SSE 解析）、`reasoning_content` 推理过程展示、图片附件（文件选择→`stageChatImages` 复制到 images 目录→base64 内联为 OpenAI `image_url` 多模态消息）、连续 user 消息合并、首条消息自动生成标题、删除会话级联清理图片、usage 上报统计。
- 数据库：`conversations` / `messages`（含 images JSON，迁移 0003）。
- **语音工作台** `voice-screen.tsx`（迁移 0004 `voice_records`）：顶部三个菜单——**语音合成 TTS / 语音识别 ASR / 声音克隆**，`stores/voice.ts` 驱动页面与侧边栏联动。
  - TTS：文本输入 + 音色选择（内置 7 个标准音色 + 用户克隆音色 chips）+ 模型输入 → 调 `POST /v1/audio/speech`（OpenAI 兼容，如 CosyVoice / F5-TTS 服务器），音频落到 images 目录并经本地 image server 播放。
  - ASR：选音频（`stageAudio` 暂存）+ 模型输入 → 调 `POST /v1/audio/transcriptions`（multipart），返回文本可复制。
  - 声音克隆：名称 + 参考音频 → `createVoiceClone` 持久化到 `VOICE_CLONES` 设置，克隆音色自动出现在 TTS 音色列表；克隆列表含试听与删除。
  - 每次 TTS/ASR/克隆都写入 `voice_records`，**侧边栏语音 App 按当前 Tab 展示对应记录列表**（时长/文本/内嵌播放器/删除）。

**遗留差距**：语音/图片 App 的**专门推理流程**部分落地——语音（TTS/ASR/克隆）已接通 OpenAI 兼容音频端点；**图片 App 仍未接入**专门生图流程（选 image 分类模型后仍走 chat/completions）。语音推理依赖外部服务支持 `/v1/audio/*` 端点，内置引擎（llama.cpp 等）暂不提供音频能力。

### 4.6 P1 集成（Launcher）✅

**需求**：为每个 CLI 工具提供"默认模型 + 启动命令"，Claude Code 支持本地/云端模式与 Opus/Sonnet/Haiku 三级映射。

**落地**（`settings.tsx` IntegrationsSettings）：
- Claude Code：本地/云端模式切换；Opus/Sonnet/Haiku 三个模型输入框；实时生成启动命令 `ANTHROPIC_BASE_URL=… ANTHROPIC_MODEL=… ANTHROPIC_API_KEY=EMPTY claude`，一键复制。
- 其余工具：Codex / OpenCode / OpenClaw / Hermes Agent / Pi / Copilot CLI，各配默认模型输入 + 命令预览（Codex/OpenCode/OpenClaw/Copilot 用 `OPENAI_BASE_URL/OPENAI_MODEL` 形式，Hermes/Pi 用 `--model --base-url` 形式）。
- 设置项持久化：`LAUNCHER_*` 系列键（settings.ts）。

**遗留差距**：命令仅预览/复制，未实现 `vllm-studio launch <tool>` 子命令直接拉起（OMLX 有 `omlx launch claude`）。

### 4.7 P1 性能（调度/内存/缓存）🟡

**需求**：调度器、内存与生命周期、KV 缓存分层归组管理。

**落地**：
- **调度器**：ctx size、GPU layers、parallel、batch、ubatch，按引擎分组在高级参数/性能页。
- **KV 缓存**：`SERVER_CACHE_TYPE_K/V` 量化类型（q8_0/q4_0/q4_1/f16）透传给 llama-server `--cache-type-k/v`。
- **生成**：Max Retries / Failure Retries / Page Concurrency（OCR 并发）。

**未落地（差距）**：预填充内存防护与防护层级、空闲超时自动卸载、模型回退路由、分块预填充、预填充优先级（速度 vs 最大上下文）、KV 热/冷分层与 SSD 溢出目录。其中部分依赖 llama.cpp 新版本能力（`--mlock`、KV cache offload 等），需要后端验证可行性后再设计。

### 4.8 P1 基准测试（吞吐量）✅

**落地**：
- 后端 `bun/benchmark.ts`：`runBenchmark` 按上下文列表扫描（默认 1K/4K/8K/16K/32K），每档：warm-up 单请求 + batchSize 并发流式请求，统计 TTFT（首个 token 延迟）、TPOT、TPS、输出 token、总耗时；支持本地/远程两种模式（远程取 `VLLM_API_BASE`）。
- 前端 `settings.tsx` BenchmarkSettings：模型选择（从已安装 GGUF 提取候选 chips + 手动输入）、生成长度、批次大小、上下文档位 chips 勾选；结果表格 + TPS 条形图。
- RPC：`runBenchmark`。

**遗留差距**：上下文×批次是多对一（一趟固定 batch），未做扫描矩阵；无 ANE 对齐提示；无准确度/质量基准；单请求超时 10 分钟较保守。

### 4.9 P1 日志查看器 🟡

**落地**（`main-layout/server-logs.tsx`）：状态徽章（stopped/starting/downloading/running/error）、**行数**显示、**复制**、清空、开始/停止/重启、ANSI 着色、自动滚动、内存 20 万字符上限、渲染端只解析尾部 3 万字符（开头提示"已截断"）、下载中状态自动识别（正则匹配进度字样）。

**遗留差距**：无日志文件切换（OMLX 的 server.log 多文件）、无"最近 N 条"显式筛选。

### 4.10 P2 通用（更新通道 / 关于 / 界面语言）✅

- **更新通道**：`UPDATE_CHANNEL`（stable/beta）；`updates.ts` 在平台支持时通过 `Updater.setChannel()` 应用所选通道后检查更新；状态机 checking/available/downloading/ready/no-update/error 广播给前端。
- **关于**：整合进设置"通用"页：版本、通道、运行时长（sessionStartedAt）、模型存储位置（basePath）、清空服务器日志（按钮）。
- **界面语言**：`UI_LANG`（zh/en），`shared/i18n.ts` 全量词典，`ui-lang` store + 设置页按钮切换，全局即时生效。

### 4.11 P2 外观（菜单栏 / Dock）❌

未实现。`bun/menu.ts` 已有应用菜单；菜单栏图标、Dock 显隐、菜单栏指标（LIV/AVG/ALL、CPU/GPU/内存条）需先评估 Electrobun 的系统托盘/菜单栏 API 支持度。

### 4.12 P2 安全 ❌

未实现。`VLLM_API_KEY` 目前明文存于 SQLite settings 表（前端输入框为 password 型但存储未加密）；无日志脱敏。规划：Keychain/系统凭据存储、日志打印前脱敏、远端 Key 不回显。

---

## 5. 与当前应用的差距分析（现状核对）

| 模块 | 状态 | 代码位置 | 说明 |
|---|---|---|---|
| 服务统计 | ✅ | `bun/stats.ts`、`server-stats.tsx`、RPC `getServerStats` | 2s 轮询；缺逐模型显存 |
| 模型分类 | ✅ | `shared/modelscope.ts`、`model-store.ts`、`models-screen.tsx` | 持久化 + 徽章 + 筛选 + 兜底 |
| 网络/服务 | ✅ | `settings.tsx`（网络页）、`runtimes/`、`server-manager.ts` | 三引擎 + 本地/远程 + 端点 + 测试连接 |
| 模型/下载器 | ✅ | `modelscope.ts`、`download-manager.ts`、`model-detail.tsx`、`download-panel.tsx` | 续传修复、队列、多目录、收藏 |
| 多 App 结构 + 聊天 | ✅ | `app-sidebar.tsx`、`stores/app.ts`、`chat.ts`、`chat-screen.tsx`、`voice-screen.tsx`、`bun/voice.ts` | 四 App 切换 + 会话隔离 + 多模态聊天 + 语音工作台（TTS/ASR/克隆） |
| 集成 Launcher | ✅ | `settings.tsx`（集成页） | 命令生成 + 三级映射；缺 `vllm-studio launch` 子命令 |
| 基准测试 | ✅ | `bun/benchmark.ts`、`settings.tsx`（基准页） | 上下文扫描；缺 batch×ctx 矩阵/ANE 提示 |
| 性能分组 | 🟡 | `settings.tsx`（性能页） | 调度/缓存量化已归组；内存生命周期未做 |
| 日志 | 🟡 | `server-logs.tsx` | 行数/复制/清空/实时已补；无文件切换/最近 N 条 |
| 更新/关于 | ✅ | `updates.ts`、`settings.tsx`（通用页） | 通道 + 关于信息块 |
| 界面多语言 | ✅ | `shared/i18n.ts`、`stores/ui-lang.ts` | 中/英 |
| 外观（托盘/Dock） | ❌ | `bun/menu.ts`（仅应用菜单） | 待 Electrobun 托盘能力评估 |
| 安全 | ❌ | `db/settings.ts`（潜在改进点） | 明文 Key、无脱敏 |

---

## 6. 实施路线图（截至 2026-09-08）

- **Phase 1 ✅**：需求文档 + 服务统计仪表盘（后端统计 + 前端页面 + 侧边栏入口）+ 模型分类（持久化 + 已安装分组筛选）。
- **Phase 2 ✅**：设置页标签重组（网络/模型/模型库/性能/集成/基准/日志/统计/通用/界面）；端点展示；模型目录多路径；下载续传修复；下载管理器（队列/暂停/重试）+ 模型详情页 + 收藏夹；三引擎运行时抽象。
- **Phase 3 ✅（为主）**：集成 Launcher（Claude Code 三级映射 + 六个 CLI 工具命令生成）；日志查看器增强（行数/复制/清空）；多 App 结构 + 多模态聊天（图片附件、会话隔离）；更新通道；通用/关于页；i18n。
- **Phase 4（规划/部分进行）**：语音工作台 ✅（TTS / ASR / 声音克隆，`/v1/audio/*` OpenAI 兼容端点 + 记录库）；待办——**图片 App 生图闭环**、性能-内存与生命周期（空闲卸载、内存防护、模型回退）、KV 缓存分层与 SSD 溢出、基准测试 batch×ctx 扫描矩阵与准确度基准、日志文件多文件切换。
- **Phase 5（远期）**：外观（菜单栏/Dock 指标）；安全（Key 加密存储、日志脱敏）；`vllm-studio launch <tool>` 子命令；逐模型显存统计。

---

## 7. 交付记录

1. 本文档（`需求文档.md`）。
2. 后端：`stats.ts`（会话级 token 统计/速度/活跃模型/系统资源/磁盘占用）；`chat.ts` + `chat-model.ts`（多模态流式聊天、会话库、usage 埋点）；`modelscope.ts`（搜索/文件/下载/续传）；`download-manager.ts`（任务队列管理 + 广播）；`model-store.ts`（目录多路径/收藏/分类持久化/激活删除）；`runtimes/`（llama.cpp/vLLM/SGLang 抽象与引擎热切换）；`benchmark.ts`（上下文扫描吞吐基准）；`server-manager.ts` 门面；RPC 全套（`rpc/index.ts`）。
3. 前端：`server-stats.tsx`、`models-screen.tsx`、`model-detail.tsx`、`chat-screen.tsx`、`download-panel.tsx`、`settings.tsx`（10 标签重组）、`app-sidebar.tsx`（App 切换 + 双列表）、`server-logs.tsx`；路由与状态 stores（router/app/chat/model-detail/model-download/ui-lang/server）。
4. 共享与国际化：`shared/modelscope.ts`（分类/预设）、`shared/server-info.ts`、`shared/i18n.ts`（中/英）、`shared/model-profiles.ts`。
5. 数据库：迁移 0001–0003（会话 App 字段、消息图片 JSON）。

---

## 8. 参考截图

来源：`~/.zcode/cli/image-cache/sess_6a6ed8d6-e8b6-41bd-8792-ad485d7a3a3a/`
（image-08ed…=集成、image-1947…=网络、image-259b…=性能、image-44a6…=外观、image-696b…=吞吐量、image-9f55…=模型、image-d3d7…=服务统计、image-eb13…=日志）
